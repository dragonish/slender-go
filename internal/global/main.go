// Package global defines global variables.
package global

import (
	"slender/internal/model"
)

// Flags is global variables.
var Flags model.Flags

// Config is user config.
var Config model.UserConfig
